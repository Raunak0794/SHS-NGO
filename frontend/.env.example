# Frontend Environment Variables
# Vite automatically prefixes with VITE_ to expose variables to the browser

# API Configuration (if needed for environment-specific API URLs)
VITE_API_URL=http://localhost:3000/api
VITE_APP_NAME=SHS NGO
